# ─────────────────────────────────────────
# ARC Raiders LFG — Required Environment Variables
# Copy this file to .env.local and fill in real values
# NEVER commit .env.local — it's in .gitignore
# ─────────────────────────────────────────

# NextAuth — Discord OAuth
# Create app at https://discord.com/developers/applications
# OAuth2 → Redirects → add: https://YOUR_DOMAIN/api/auth/callback/discord
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# NextAuth secret — generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://YOUR_VERCEL_DOMAIN.vercel.app

# Database
# Local dev:   DATABASE_URL="file:./prisma/dev.db"
# Vercel/Prod: DATABASE_URL="mysql://user:pass@host:3306/escalation"
#              (use PlanetScale, Railway, or Supabase)
DATABASE_URL=

# App
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://YOUR_VERCEL_DOMAIN.vercel.app
